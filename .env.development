POSTGRES_USER=local_user
POSTGRES_PASSWORD=local_password
POSTGRES_DB=local_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# para rodar sem erro no "npm run dev" essa ordem deve se manter, os 3 primeiros definem as informações mais importantes do docker
